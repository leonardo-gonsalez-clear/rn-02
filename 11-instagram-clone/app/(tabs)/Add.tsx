import { View, Text } from 'react-native'
import React from 'react'
import AddPhoto from '../../interfaces/AddPhoto/AddPhoto'

const Add = () => {
  return (
    <AddPhoto />
  )
}

export default Add
